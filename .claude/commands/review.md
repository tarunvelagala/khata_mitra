Review the most recent changes in KhataPro as a senior Flutter engineer and mentor.

Look at `git diff HEAD~1 HEAD` and the current state of changed files.

Apply the code-reviewer agent standards — check for magic numbers, semantic color usage, shared widgets, Riverpod-only state, go_router-only navigation, zero hard-coded strings, AsyncValue completeness, and dart analyze cleanliness.

End with a verdict: APPROVE, APPROVE WITH SUGGESTIONS, or REQUEST CHANGES.
