/// Asset path constants for KhataPro.
///
/// Always reference assets through these constants — never inline path strings.
abstract final class AppAssets {
  /// Main app logo SVG used in branding screens (language, theme selection).
  static const String appIcon = 'assets/icons/app_icon.svg';
}
